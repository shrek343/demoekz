<?php
// ============================================================
//  ШАПКА (общая для всех страниц).
//  Логотип и название компании задаются прямо здесь.
//  Цвета и шрифт сайта — в файле css/style.css (вверху, блок :root).
// ============================================================

require_once __DIR__ . '/auth.php';   // подключаем авторизацию: нужны isLoggedIn() и session_start()
                                       // (путь без /includes/ — мы УЖЕ внутри папки includes)
$active = $active ?? '';               // подстраховка: если страница не задала $active, ставим пусто (без ошибки)
?>
<!DOCTYPE html>                        <!-- объявляем, что это HTML5 -->
<html lang="ru">                       <!-- язык страницы — русский (важно для поиска и доступности) -->
<head>
  <meta charset="UTF-8">               <!-- кодировка UTF-8 — чтобы кириллица не превратилась в «кракозябры» -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0"><!-- адаптация под смартфон 390x844 -->

  <!-- ✏️ НАЗВАНИЕ ВКЛАДКИ БРАУЗЕРА -->
  <title>Название вашей компании</title>   <!-- текст на вкладке браузера -->

  <!-- порядок CSS важен: сначала фреймворк, потом свои стили — чтобы СВОИ перебивали стандартные -->
  <link rel="stylesheet" href="css/bootstrap.min.css"><!-- фреймворк -->
  <link rel="stylesheet" href="css/fonts.css">        <!-- шрифты -->
  <link rel="stylesheet" href="css/style.css">        <!-- общие стили + цвета (меняй цвета там) -->
</head>
<body>
<div class="app-shell"><!-- "экран смартфона" по центру: узкий контейнер под мобильный вид -->

  <header class="app-header">
    <a href="index.php" class="brand">       <!-- весь логотип+название — ссылка на главную -->
      <!-- ✏️ ЛОГОТИП: положи свой файл в img/logo.png (или поменяй путь) -->
      <img src="img/logo.png" alt="Логотип" class="brand-logo">   <!-- alt — текст, если картинка не загрузится -->
      <!-- ✏️ НАЗВАНИЕ КОМПАНИИ -->
      Название вашей компании
    </a>

    <span class="small">                      <!-- правый блок шапки мелким шрифтом -->
      <?php if (isLoggedIn()): ?>
        <!-- пользователь вошёл -> показываем его ФИО и ссылку «Выйти». e() экранирует ФИО (защита от XSS) -->
        <?= e($_SESSION['fio']) ?> &middot; <a href="logout.php">Выйти</a>  <!-- &middot; — точка-разделитель · -->
      <?php else: ?>
        <!-- гость -> только ссылка «Вход» -->
        <a href="login.php">Вход</a>
      <?php endif; ?>
    </span>
  </header>

  <main class="app-body fade-up">   <!-- контейнер под содержимое страницы; fade-up — анимация появления -->
  <!-- ВНИМАНИЕ: <main>, <div.app-shell> и <body> здесь НЕ закрыты — их закроет footer.php -->